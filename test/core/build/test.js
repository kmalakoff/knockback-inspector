// Generated by CoffeeScript 1.3.3
(function() {

  $(document).ready(function() {
    var Backbone, kb, kbi, ko, _, _ref;
    module("knockback-inspector");
    _ = !window._ && (typeof require !== 'undefined') ? (_ref = require('underscore')) != null ? _ref._ : void 0 : window._;
    Backbone = !window.Backbone && (typeof require !== 'undefined') ? require('backbone') : window.Backbone;
    ko = !window.ko && (typeof require !== 'undefined') ? require('knockout') : window.ko;
    kb = !window.kb && (typeof require !== 'undefined') ? require('knockback') : window.kb;
    kbi = !window.kbi && (typeof require !== 'undefined') ? require('knockback-inspector') : window.kbi;
    test("TEST DEPENDENCY MISSING", function() {
      ok(!!ko);
      ok(!!_);
      ok(!!Backbone);
      ok(!!kb);
      return ok(!!kbi);
    });
    window.kbi = kbi;
    ko.setTemplateEngine(new kbi.StringTemplateEngine());
    test("Backbone.Model", function() {
      var html, your_model;
      html = "<ul id='model' data-bind=\"template: {name: 'kbi_model_node', data: kbi.nvm('root', true, $data)}\"></ul>";
      your_model = new Backbone.Model({
        name: 'Hello',
        place: 'World!'
      });
      return ko.applyBindings(kb.viewModel(your_model), $(html)[0]);
    });
    test("Backbone.Collection", function() {
      var html, your_collection;
      html = "<ul id='collection' data-bind=\"template: {name: 'kbi_collection_node', data: kbi.nvm('root', true, $data)}\"></ul>";
      your_collection = new Backbone.Collection([
        {
          name: 'Hello',
          place: 'World!'
        }, {
          name: 'Goodbye',
          place: 'Samsara!'
        }
      ]);
      return ko.applyBindings(kb.collectionObservable(your_collection, {
        view_model: kb.ViewModel
      }), $(html)[0]);
    });
    return test("Error cases", function() {});
  });

}).call(this);
